LABWC DOTFILES IMPLEMENTATION - STATUS SUMMARY

✅ CURRENT STATE:
   - System configuration: ~/.config/labwc (already has rc.xml etc.)
   - Project dotfiles: ./dotfiles/labwc/rc.xml (contains our fixes)
   - Sync tool: dotfiles-sync.sh handles two-way sync

✅ FIXES IMPLEMENTED:
   1. Context menu right-click configuration:
      - Titlebar Top Right Bottom Left contexts: right-click shows client-menu
      - Title context: right-click shows client-menu  
      - Root context: right-click shows root-menu

   2. Double-tap / double-click separation:
      - <default/> loads standard labwc mouse bindings
      - No custom double-tap/double-click overrides
      - Standard behavior maintained

   3. Tap configuration:
      - tapButtonMap: lrm (left/middle/right) - correct mapping
      - libinput touchpad configuration

✅ IMPLEMENTATION TOOLS:
   - dotfiles-sync.sh: Two-way sync script
   - git-based management
   - Consistent with standard labwc configuration

WHAT TO DO:
   1. Test current configuration (right-click menu)
   2. Deploy project dotfiles with: dotfiles-sync.sh push
   3. Verify system behavior matches project configuration

Two-way sync available:
   dotfiles-sync.sh diff    # Show differences
   dotfiles-sync.sh push   # Project → System
   dotfiles-sync.sh pull   # System → Project
